# @retroport/schemas

Shared TypeScript contracts and Zod runtime validation schemas for manifests,
evidence, agent results, hypotheses, experiments, compatibility, and the initial
horizontal-movement semantic IR.
